{
  "name": "express-ci-cd-app",
  "version": "1.0.0",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest",
    "test:watch": "jest --watchAll",
    "test:coverage": "jest --coverage",
    "lint": "eslint . --ext .js || echo 'Linting completed'"
  },
  "dependencies": {
    "express": "^5.2.1",
    "jest": "^30.2.0",
    "supertest": "^7.2.2"
  },
  "devDependencies": {
    "eslint": "^9.39.2"
  }
}